import { ref, computed } from "vue";
import type {
  NotificationConfig,
  ActiveNotification,
  Position,
} from "../types/toast";
import { generateId } from "../utils/nanoid";
// ...existing code...
